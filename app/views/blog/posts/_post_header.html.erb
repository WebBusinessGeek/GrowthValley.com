<div class="post-header">
<h1><%= link_to post.title, post.full_url %></h1>
<div class="posted"> 
  <span>
    <time datetime="<%= post.published_at %>"> <%= I18n.localize(post.published_at.to_date, format: :long) %> </time>
  </span> 
  <span><%= post.user.name %></span> 
</div>
</div>