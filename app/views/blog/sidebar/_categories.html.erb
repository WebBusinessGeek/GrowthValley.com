<%= sidebar_section_for(t(".title")) do %>
   <ul>
     <% @tags.each do |tag| %>
       <li id="tag"><%=link_to "#{tag.name} (#{tag.frequency})", blog_tags_page_path(tag: tag.name) %> </li>
     <% end %>
   </ul>
 <% end %>