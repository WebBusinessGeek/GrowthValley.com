<%= sidebar_section_for(t(".title")) do  %>
  <% @tags.each do |tag| %>
    <%= link_to tag.name, blog_tags_page_path(tag: tag.name), { class: label_for_tag(tag,@tags_frequency_min,@tags_frequency_max)} %>
  <% end %>
<% end  %>